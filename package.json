{
  "name": "pathforge",
  "version": "0.1.0",
  "private": true,
  "description": "PathForge — Career Intelligence for Everyone",
  "author": "PathForge <hello@pathforge.eu>",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev:web": "pnpm --filter pathforge-web dev",
    "build:web": "pnpm --filter pathforge-web build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}
